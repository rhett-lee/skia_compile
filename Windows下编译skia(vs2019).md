# Windows系统中使用LLVM编译Skia源码的方法（VS  2019） - 修改日期：2026-04-12 - 操作系统：Windows 10 64位版本 - Windows SDK：10.0.22621.0版本（该版本为Windows 11 SDK，因为nim_duilib的CEF模块需要依赖Windows 11 SDK，Windows 10 SDK会导致CEF相关模块编译不过；如果不使用CEF功能，使用Windows 10 SDK也可以） - Microsoft Visual Studio：2019 社区版(v16.11.54) - 编译器：LLVM 21.1.4 Win64 版本 - 说明1：本文档介绍Windows系统中使用LLVM编译Skia源码的方法 - 说明2：该编译Skia源码的方法，是为了适配[nim_duilib](https://github.com/rhett-lee/nim_duilib)项目使用Skia库，如果用于其他库使用，可能需要修改编译参数 - 说明3：为了提升Debug版本的运行性能，在Debug版本的编译中，使用Release版本相同的编译参数，只是把运行时库从/MT换成/MTd。 - 说明4：获取skia源码后，需要更新部分源码（更新方法见后续文档），否则编译无法通过。（使用了一个第三方库：expat） - 说明5：操作过程中，假设源码的根目录是`D:\develop`目录，如果使用其他目录，可替换为实际的目录。## 一、准备工作：安装必备的软件1. 安装python3（python的主版本需要是3，需要添加到Path环境变量）    （1）首先安装python3    （2）到python.exe所在目录中，复制一份python.exe，改名为python3.exe: 确保命令行参数中可以访问到python3.exe   （3）在命令行验证：`> python3.exe --version` 可以查看python3的版本号     2. 安装Git For Windows: 2.53版本（其他版本也可以），git需要添加到Path环境变量，确保命令行参数中可以访问到git.exe    3. 安装Visual Studio 2019社区版    -  如果本机同时存在多个版本的Visual Studio，默认会选择版本最高的Visual Studio。       如果需要修改编译的Visual Studio版本，可使用win_vc参数指定VS的具体路径（需手动编译），如下：       `--args="win_vc=\"C:\\Program Files\\Microsoft Visual Studio\\2022\\Community\\VC\" target_cpu=\"x64\" ... `    -  如果本机安装了多个版本的Windows SDK，默认会选择版本最高的Windows SDK。       如果需要指定Windows SDK的版本，可使用win_sdk_version参数指定具体版本号（需手动编译），如下：       `--args="win_sdk_version=\"10.0.26100.0\" target_cpu=\"x64\" ... `       Windows SDK默认安装路径为`C:\Program Files (x86)\Windows Kits\10\Include`，本机安装的Windows SDK版本均可在这个目录中查看。    - 如果Visual Studio安装了多个版本的工具集，默认会选择版本最高的工具集。      如果需要指定工具集版本，可使用win_toolchain_version参数指定具体工具集版本号（需手动编译），如下：       `--args="win_toolchain_version=\"14.51.36231\" target_cpu=\"x64\" ... `    4. 安装LLVM：21.1.4 Win64 版本    （1）安装目录：`C:\LLVM`    （2）注意事项：安装目录中，不能有空格，否则编译会遇到问题。    ## 二、使用脚本自动编译（推荐）该脚本自动完成相关源码下载和编译工作。    选定一个工作目录，创建一个脚本`build_skia.bat`，将下面已经整理好脚本复制进去，保存文件。    - 脚本文件内容如下：    ```echo OFFset retry_delay=10:retry_clone_skia_compileif not exist ".\skia_compile\.git" (    git clone https://github.com/rhett-lee/skia_compile) else (    git -C ./skia_compile pull)if %errorlevel% neq 0 (    timeout /t %retry_delay% >nul    goto retry_clone_skia_compile)if not exist ".\skia_compile\.git" (    echo clone skia_compile failed!    exit /b 1):retry_pull_skia_compilegit -C ./skia_compile checkout develop-cpp17git -C ./skia_compile pullif %errorlevel% neq 0 (    timeout /t %retry_delay% >nul    goto retry_pull_skia_compile).\skia_compile\windows\build_skia_all_in_one.bat```上述脚本默认编译使用的是静态运行库(MT和MTd)，如果需要使用动态运行库（MD和MDd），需要将上述脚本的最后一行追加`/MD`参数，改为：    `.\skia_compile\windows\build_skia_all_in_one.bat /MD`    备注：如果nim_duilib最终编译为DLL库，那么就必须使用动态运行库。    - 进入命令行控制台，运行该脚本： ```.\build_skia.bat```   编译完成的库文件在工作目录的`skia/out`子目录中，按编译选项放在相应的子目录。    ## 三、手动编译过程### 第1步：获取skia源码和修改的源码1. 获取skia源码：    （1）\> `mkdir D:\develop`    （2）\> `cd /d D:\develop`    （3）\> `git clone https://github.com/google/skia.git`    （4）\> `git checkout 34aa71b8bee4648a442b7125680232d803374f19`    2. 应用修改的代码：    （1）\> `cd /d D:\develop`    （2）\> `git clone https://github.com/rhett-lee/skia_compile`（下载源码和文档）    （3）\> `git checkout develop-cpp17`（切换到支持C++17的分支）    （4） 解压`skia.2026-02-10.src.zip`到目录`skia.2026-02-10.src`中    （5） 将目录`skia.2026-02-10.src`中的所有内容，复制到`D:\develop\skia`目录中，覆盖所有同名文件    （6） 注意事项：该修改的代码的SHA-1值，需要比对，如果不是这个版本的代码，直接覆盖可能有问题。    ### 第2步：编译skia（编译器：LLVM）#### （1）编译64位的Lib（x64版本）1. 首先进入命令行提示符（cmd.exe）2. 进入skia源码目录：    \> `cd /d D:\develop\skia`    3. 编译skia库（llvm.x64.debug，默认为静态运行库，如需动态运行库，可以手动将命令行参数中的`/MTd`改为`/MDd`） - `.\bin\gn.exe gen out/llvm.x64.debug --ide="vs2019" --sln="skia" --args="target_cpu=\"x64\" cc=\"clang\" cxx=\"clang++\" clang_win=\"C:/LLVM\" is_trivial_abi=false is_official_build=true skia_use_libwebp_encode=false skia_use_libwebp_decode=false skia_use_libpng_encode=false skia_use_libpng_decode=false skia_use_zlib=false skia_use_libjpeg_turbo_encode=false skia_use_libjpeg_turbo_decode=false skia_enable_fontmgr_win_gdi=false skia_use_icu=false skia_use_expat=false skia_use_xps=false skia_enable_pdf=false skia_use_wuffs=false skia_enable_svg=true skia_use_expat=true skia_use_system_expat=false is_debug=false extra_cflags=[\"-DSK_DISABLE_LEGACY_PNG_WRITEBUFFER\",\"/MTd\"]"`     - `.\bin\ninja.exe -C out/llvm.x64.debug`    （如果需要重新编译，可以先用命令清理：`.\bin\ninja.exe -C out/llvm.x64.debug -t clean`）    4. 编译skia库（llvm.x64.release，默认为静态运行库，如需动态运行库，可以手动将命令行参数中的`/MT`改为`/MD`） - `.\bin\gn.exe gen out/llvm.x64.release --ide="vs2019" --sln="skia" --args="target_cpu=\"x64\" cc=\"clang\" cxx=\"clang++\" clang_win=\"C:/LLVM\" is_trivial_abi=false is_official_build=true skia_use_libwebp_encode=false skia_use_libwebp_decode=false skia_use_libpng_encode=false skia_use_libpng_decode=false skia_use_zlib=false skia_use_libjpeg_turbo_encode=false skia_use_libjpeg_turbo_decode=false skia_enable_fontmgr_win_gdi=false skia_use_icu=false skia_use_expat=false skia_use_xps=false skia_enable_pdf=false skia_use_wuffs=false skia_enable_svg=true skia_use_expat=true skia_use_system_expat=false is_debug=false extra_cflags=[\"-DSK_DISABLE_LEGACY_PNG_WRITEBUFFER\",\"/MT\"]"`     - `.\bin\ninja.exe -C out/llvm.x64.release` #### （2）编译32位的Lib（x86版本）1. 首先进入命令行提示符（cmd.exe）2. 进入skia源码目录：    \> `cd /d D:\develop\skia`     3. 编译skia库（llvm.x86.release，默认为静态运行库，如需动态运行库，可以手动将命令行参数中的`/MT`改为`/MD`） - `.\bin\gn.exe gen out/llvm.x86.release --ide="vs2019" --sln="skia" --args="target_cpu=\"x86\" cc=\"clang\" cxx=\"clang++\" clang_win=\"C:/LLVM\" is_trivial_abi=false is_official_build=true skia_use_libwebp_encode=false skia_use_libwebp_decode=false skia_use_libpng_encode=false skia_use_libpng_decode=false skia_use_zlib=false skia_use_libjpeg_turbo_encode=false skia_use_libjpeg_turbo_decode=false skia_enable_fontmgr_win_gdi=false skia_use_icu=false skia_use_expat=false skia_use_xps=false skia_enable_pdf=false skia_use_wuffs=false skia_enable_svg=true skia_use_expat=true skia_use_system_expat=false is_debug=false extra_cflags=[\"-DSK_DISABLE_LEGACY_PNG_WRITEBUFFER\",\"/MT\"]"`     - `.\bin\ninja.exe -C out/llvm.x86.release`4. 编译skia库（llvm.x86.debug，默认为静态运行库，如需动态运行库，可以手动将命令行参数中的`/MTd`改为`/MDd`） - `.\bin\gn.exe gen out/llvm.x86.debug --ide="vs2019" --sln="skia" --args="target_cpu=\"x86\" cc=\"clang\" cxx=\"clang++\" clang_win=\"C:/LLVM\" is_trivial_abi=false is_official_build=true skia_use_libwebp_encode=false skia_use_libwebp_decode=false skia_use_libpng_encode=false skia_use_libpng_decode=false skia_use_zlib=false skia_use_libjpeg_turbo_encode=false skia_use_libjpeg_turbo_decode=false skia_enable_fontmgr_win_gdi=false skia_use_icu=false skia_use_expat=false skia_use_xps=false skia_enable_pdf=false skia_use_wuffs=false skia_enable_svg=true skia_use_expat=true skia_use_system_expat=false is_debug=false extra_cflags=[\"-DSK_DISABLE_LEGACY_PNG_WRITEBUFFER\",\"/MTd\"]"`     - `.\bin\ninja.exe -C out/llvm.x86.debug`   ## 四、为何使用LLVM编译Skia    因为LLVM编译的代码，执行性能明显比VS好。    用VS编译的代码，运行时有明显卡顿现象。    ## 五、资源链接1. Skia的编译文档库，如需获取最新版本的文档，请点击访问：[skia_compile](https://github.com/rhett-lee/skia_compile)     2. nim_duilib界面库的代码库，请点击访问：[nim_duilib](https://github.com/rhett-lee/nim_duilib)     nim_duilib 是一款基于C++开发的跨平台界面库，源于经典的 duilib 界面库并进行了深度优化与功能扩展，支持Windows/Linux/macOS/FreeBSD平台，支持的Linux系统包括OpenEuler、OpenKylin、UbuntuKylin、统信UOS、中科方德、Ubuntu、Fedora、Debian等，专注于简化桌面应用的高效开发。其设计融合了DirectUI理念，通过XML描述界面布局，实现视觉与逻辑的分离，显著提升开发灵活性与维护性。